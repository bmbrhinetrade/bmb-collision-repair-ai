{
  "name": "bmb-collision-repair-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Photo-driven collision estimator with VIN decode, AI vision analysis, and PDF export",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "openai": "^5.20.1",
    "pdfkit": "^0.15.2"
  }
}
